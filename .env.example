# Local Python quip service endpoint
VITE_QUIP_ENDPOINT=http://127.0.0.1:8787/api/bot-quips

# Never expose secrets in frontend files.
# Set GROQ_API_KEY in your terminal before starting server/groq_quip_server.py
